import 'package:flutter/material.dart';
import 'package:quiz_app/quize.dart';

// Void mean it does not return anything or any widget that is why we used (void) before the function
// but if any function return a widget then we used (Widget) before function like: Widget build (){}
void main() {
  runApp(const Quiz());
}
